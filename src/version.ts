/**
 * App version — update this with every meaningful release.
 *
 * Scheme: MAJOR.MINOR.PATCH
 *   MAJOR — крупная переработка / breaking change
 *   MINOR — новая фича
 *   PATCH — фикс бага / мелкое улучшение
 *
 * History:
 *   1.0.0  — базовый запуск: дизеринг, палитра, экспорт PNG / litematic
 *   1.1.0  — artist mode: слои, кисть, ластик, заливка, текст, смена опасити
 *   1.2.0  — настройки по слоям (mapMode/staircaseMode/dithering) + гибридный litematic экспорт
 *   1.3.0  — две кнопки экспорта в artist mode: LITEMATIC (все слои) и СЛОЙ (текущий)
 *   1.3.1  — fix: дизеринг и режимы снова применяются к изображению в artist mode
 *   1.4.0  — инструменты выделения: прямоугольник (R), лассо (L), волшебная палочка (W), пиксель
 *            операции: удалить, залить, инвертировать, перенести на новый слой
 *            горячие клавиши: Ctrl+A/D/I, Del, Esc
 *   1.4.1  — fix: при смене дизеринга/режима на отредактированном слое сохраняется прозрачность
 *            (удалённый фон не восстанавливается, новые настройки применяются к видимой части)
 *            fix: превью выделения во время drag; иконки инструментов выделения
 *   1.4.2  — fix: isDirty корректно наследуется при "перенести на новый слой", mergeDown, mergeVisible
 *   1.5.0  — инструмент «Паттерн» (P): тайловая кисть с редактором паттерна (перетаскиваемый попап),
 *            ползунок размера кисти для паттерна; инструмент «Градиент» (G): drag для направления,
 *            цветовые стопы, интерполяция в OKLab, дизеринг Байера 4×4
 *   1.6.0  — i18n (RU/EN), устранение лага кисти, улучшение дизайна UI
 *   1.7.0  — история проектов (IndexedDB), сохранение/загрузка, глобальный drag-and-drop
 *   1.8.0  — фильтрация палитры по версии MC (1.12.2 → 1.21.4), блоки 1.21.4,
 *            улучшение экспорта материалов (кнопка ↓ в списке, учёт макс/карта),
 *            версия-селектор над палитрой, сворачиваемая панель экспорта
 *   1.9.0  — мобильная вкладка canvas, онбординг-стрелка, анимации пустого состояния,
 *            превью дизеринга, progress bar; устранение лага split line, select overlay,
 *            hover tooltip — прямые DOM-обновления без React re-render
 *   1.10.0 — импорт/экспорт MAP.DAT, GIF-проекты и GIF LITEMATIC PACK,
 *            build tracker для команды, панель обновлений, компактная шапка,
 *            восстановление публичных share links
 *   1.10.1 — визуальный редизайн: цельные Iconify-иконки, улучшенные панели,
 *            отступы, контраст, мобильная читаемость
 *   1.11.0 — сворачиваемые панели настроек/палитры, плавный zoom относительно курсора,
 *            перемещение холста инструментом-курсором без всплывающей подсказки при drag,
 *            ускоряемые удержанием ± коррекции
 *   1.12.0 — единая система иконок: Radix для микроконтролов, Tabler для рабочих действий,
 *            Pixelarticons для крупных игровых акцентов; улучшена семантика и доступность кнопок
 *   1.13.0 — общий workshop-дизайн для всех публичных и облачных страниц,
 *            честная галерея source→result, адаптивность, a11y и ленивая загрузка маршрутов
 *   1.13.1 — корректное открытие каталога и карточек примеров с завершающим слэшем
 *            на GitHub Pages
 *   1.13.2 — короткое и понятное описание обновления в плашке редактора
 *   1.13.3 — исправлены click/drag курсора и прокрутка публичных облачных страниц
 *   1.14.0 — единый motion-язык: плавные панели, меню, модалки, списки,
 *            действия, прогресс и доступный режим уменьшенного движения
 *   1.15.0 — Remotion-анимация пустого холста: арт превращается в пиксельную карту,
 *            есть пауза и статичный кадр для reduced motion
 *   1.16.0 — временная рука Space + ЛКМ и локальное автосохранение текущей работы
 *   1.16.1 — Lens: меньше фонового трафика, защита от фантомных сессий и тяжёлых превью
 *   1.16.2 — новый логотип MapKluss, favicon и заставка ссылок для соцсетей
 *   1.17.0 — экспериментальный Two-layer: двухслойная Litematic, план и Cloud-артефакты
 *   1.17.1 — безопасный Two-layer: карта убирается между точками, четыре точки вместо пяти,
 *            проверка NW-границы и инвариантных пикселей, запрет изменяющихся блоков
 *   1.17.2 — Two-layer сохраняет ось каждого бревна и запрещает меняющийся под слоем nylium
 *   1.18.0 — встроенный пресет стабильных блоков для мгновенной добычи в Two-layer
 *   1.19.0 — публичный Two-layer с безопасной палитрой, закреплёнными Cloud-артефактами
 *            и обновлённым пошаговым интерфейсом Companion
 *   1.19.1 — Two-layer экспортирует только конструкцию арта, без основания, дорожек и разметки
 *   1.19.2 — Companion автоматически и безопасно выдаёт карту только на контрольных точках
 *   1.19.3 — Two-layer усиливает безопасность карты/Cloud-пары и честно показывает настройки блока перепада
 *   1.20.0 — полностью автоматическая защищённая карта на контрольных точках и атомарное приватное сохранение Cloud
 *   1.20.1 — стабильная палитра Two-layer, ясный статус данных карты и безопасный Cloud-backfill
 *   1.21.0 — большие Two-layer арты: детерминированный bundle всех карт, Cloud и выбор части в Companion;
 *            компактная плашка MapKluss Companion с гайдом в Telegram
 *   1.21.1 — обычное обновление страницы очищает холст редактора и не восстанавливает предыдущий арт
 *   1.21.2 — Two-layer показывает гайд перед первым запуском; быстрый пресет корректно применяется
 *            и включает блоки, которые мгновенно ломаются рукой
 *   1.22.0 — десять переключаемых тем интерфейса: шесть тёмных и четыре светлых,
 *            собственные поверхности, контрастная иерархия дополнительных тем и workspace редактора;
 *            локальное сохранение без вспышки и без визуальных изменений MapKluss Classic
 *   1.23.0 — безопасная и единая панель инструментов редактора: контекстные настройки и подсказки,
 *            настоящая блокировка слоёв, корректные многослойные градиенты/выделения и структурный Undo
 *   1.23.1 — выбор блока возвращён в настройки паттерн-кисти и убран из градиента,
 *            где цвета задаются собственными точками градиента
 *   1.23.2 — сетка редактора паттерна точно соответствует выбранным ширине и высоте
 *            без ложнего неактивного столбца и остаточной области справа
 *   1.24.0 — Minecraft 26.2 в редакторе, новые блоки серы/киновари и экспорты с DataVersion 4903;
 *            Companion 0.8.0 для 26.2, 1.21.11, 1.21.8 и 1.21.4
 *   1.25.0 — отдельная адаптивная Wiki с поиском и актуальными разделами;
 *            три коротких интерактивных тура по задачам вместо старого общего тура;
 *            компактная Remotion-заставка со стопкой карт и знаком MapKluss для всех тем
 *   1.25.1 — логотип начинает новый арт, а обновление страницы после Cloud-сохранения
 *            больше не сохраняет связь с предыдущим облачным артом
 *   1.26.0 — после успешного экспорта появляется ненавязчивая поддержка автора;
 *            постоянная кнопка поддержки убрана из шапки, а Analytics отделяет способ
 *            загрузки от источника кампании и очищает landing path
 *   1.26.1 — на странице Cloud опубликован MapKluss Companion 0.11.0:
 *            полностью обновлённый интерфейс, улучшенные AutoFrame, Lens и Two-layer
 *   1.26.2 — сохранение арта и загрузка Cloud ускорены пакетными операциями;
 *            подготовлен Companion 0.11.1 без повторного скачивания актуальных схем и превью
 *   1.26.3 — Remotion-заставка и строка обновления снова движутся при системном режиме
 *            уменьшенной анимации; строка непрерывна на широких экранах, а стрелки боковых
 *            панелей больше не прыгают при наведении
 *   1.26.4 — постоянная кнопка поддержки на Boosty возвращена в шапку редактора
 *   1.26.5 — Wiki, примеры и SEO-руководства получают собственный статический HTML и адреса без 404;
 *            sitemap дополнен, а редактор связывает полезные страницы настоящими внутренними ссылками
 *   1.26.6 — страницы коллекций работают с текущим и предыдущим API без отдельного backend rollout;
 *            приватные и недоступные арты больше не показывают пользователю технический текст Edge Function
 *   1.26.7 — прямые ссылки на коллекции и приватные арты дожидаются восстановления Cloud-сессии
 *   1.26.8 — коллекции загружают состав через защищённые таблицы и рабочий Cloud-снимок без зависимости от старого Edge-обработчика
 *   1.26.9 — исправлено имя даты добавления арта в коллекцию в сайте и исходнике Companion API
 *   1.26.10 — gateway-first проверяет полный readiness-ответ, staging отделён от production,
 *             а мониторинг постоянно проверяет и gateway, и прямой запасной маршрут
 *   1.26.11 — Cloud, Device и Art получили чистые подписи форм, устойчивые заголовки и
 *             нормальные области клика; гостевой Telegram больше не дублирует заглушку
 *   1.26.12 — история версий арта показывает текущую версию, понятные даты, доступные
 *             файлы и безопасное восстановление старого проекта через редактор
 *   1.26.13 — сканы из Companion открываются как новые проекты с честным сценарием
 *             сохранения, устойчивой привязкой к арту, превью и быстрым удалением
 *   1.26.14 — Wiki и интерактивные туры описывают только выпущенные функции Cloud,
 *             Companion, Lens и Two-layer без скрытых инструментов и лишних обещаний
 *   1.26.15 — Analytics, Clarity и тяжёлая Remotion-анимация загружаются после первого экрана,
 *             не мешая запуску редактора; очередь событий и живая заставка сохранены
 *   1.26.16 — Analytics измеряет безопасную Cloud-воронку, повторное открытие артов, скачивание Companion,
 *             показы поддержки и версии релиза; сырые сообщения, stack trace и идентификаторы ошибок больше не отправляются
 *   1.26.17 — облачные арты сохраняются с кириллическими названиями, небольшие сохранения обрабатываются
 *             параллельнее, а форма сохранения больше не показывает текст о будущей галерее
 *   1.27.1 — на сайте опубликован Companion 0.13.1 с новым интерфейсом, шрифтом Inter,
 *            добровольной анонимной статистикой и оптимизированным Lens
 *   1.28.0 — экспортные файлы одной работы дополнительно и приватно архивируются
 *            в отдельной короткоживущей сессии для проверки результата
 *   1.28.1 — исправлен фоновый upload закрытого архива для гостевого экспорта
 */
// 1.28.1: guest export archives bind signed uploads to their reserved artifact.
export const VERSION = 'v1.28.1';
