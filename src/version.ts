/**
 * App version — update this with every meaningful release.
 *
 * Scheme: MAJOR.MINOR.PATCH
 *   MAJOR — крупная переработка / breaking change
 *   MINOR — новая фича
 *   PATCH — фикс бага / мелкое улучшение
 *
 * History:
 *   1.0.0  — базовый запуск: дизеринг, палитра, экспорт PNG / litematic
 *   1.1.0  — artist mode: слои, кисть, ластик, заливка, текст, смена опасити
 *   1.2.0  — настройки по слоям (mapMode/staircaseMode/dithering) + гибридный litematic экспорт
 *   1.3.0  — две кнопки экспорта в artist mode: LITEMATIC (все слои) и СЛОЙ (текущий)
 *   1.3.1  — fix: дизеринг и режимы снова применяются к изображению в artist mode
 *   1.4.0  — инструменты выделения: прямоугольник (R), лассо (L), волшебная палочка (W), пиксель
 *            операции: удалить, залить, инвертировать, перенести на новый слой
 *            горячие клавиши: Ctrl+A/D/I, Del, Esc
 *   1.4.1  — fix: при смене дизеринга/режима на отредактированном слое сохраняется прозрачность
 *            (удалённый фон не восстанавливается, новые настройки применяются к видимой части)
 *            fix: превью выделения во время drag; иконки инструментов выделения
 *   1.4.2  — fix: isDirty корректно наследуется при "перенести на новый слой", mergeDown, mergeVisible
 *   1.5.0  — инструмент «Паттерн» (P): тайловая кисть с редактором паттерна (перетаскиваемый попап),
 *            ползунок размера кисти для паттерна; инструмент «Градиент» (G): drag для направления,
 *            цветовые стопы, интерполяция в OKLab, дизеринг Байера 4×4
 *   1.6.0  — i18n (RU/EN), устранение лага кисти, улучшение дизайна UI
 *   1.7.0  — история проектов (IndexedDB), сохранение/загрузка, глобальный drag-and-drop
 *   1.8.0  — фильтрация палитры по версии MC (1.12.2 → 1.21.4), блоки 1.21.4,
 *            улучшение экспорта материалов (кнопка ↓ в списке, учёт макс/карта),
 *            версия-селектор над палитрой, сворачиваемая панель экспорта
 *   1.9.0  — мобильная вкладка canvas, онбординг-стрелка, анимации пустого состояния,
 *            превью дизеринга, progress bar; устранение лага split line, select overlay,
 *            hover tooltip — прямые DOM-обновления без React re-render
 *   1.10.0 — импорт/экспорт MAP.DAT, GIF-проекты и GIF LITEMATIC PACK,
 *            build tracker для команды, панель обновлений, компактная шапка,
 *            восстановление публичных share links
 *   1.10.1 — визуальный редизайн: цельные Iconify-иконки, улучшенные панели,
 *            отступы, контраст, мобильная читаемость
 *   1.11.0 — сворачиваемые панели настроек/палитры, плавный zoom относительно курсора,
 *            перемещение холста инструментом-курсором без всплывающей подсказки при drag,
 *            ускоряемые удержанием ± коррекции
 *   1.12.0 — единая система иконок: Radix для микроконтролов, Tabler для рабочих действий,
 *            Pixelarticons для крупных игровых акцентов; улучшена семантика и доступность кнопок
 *   1.13.0 — общий workshop-дизайн для всех публичных и облачных страниц,
 *            честная галерея source→result, адаптивность, a11y и ленивая загрузка маршрутов
 *   1.13.1 — корректное открытие каталога и карточек примеров с завершающим слэшем
 *            на GitHub Pages
 *   1.13.2 — короткое и понятное описание обновления в плашке редактора
 *   1.13.3 — исправлены click/drag курсора и прокрутка публичных облачных страниц
 *   1.14.0 — единый motion-язык: плавные панели, меню, модалки, списки,
 *            действия, прогресс и доступный режим уменьшенного движения
 *   1.15.0 — Remotion-анимация пустого холста: арт превращается в пиксельную карту,
 *            есть пауза и статичный кадр для reduced motion
 *   1.16.0 — временная рука Space + ЛКМ и локальное автосохранение текущей работы
 *   1.16.1 — Lens: меньше фонового трафика, защита от фантомных сессий и тяжёлых превью
 *   1.16.2 — новый логотип MapKluss, favicon и заставка ссылок для соцсетей
 *   1.17.0 — экспериментальный Two-layer: двухслойная Litematic, план и Cloud-артефакты
 *   1.17.1 — безопасный Two-layer: карта убирается между точками, четыре точки вместо пяти,
 *            проверка NW-границы и инвариантных пикселей, запрет изменяющихся блоков
 *   1.17.2 — Two-layer сохраняет ось каждого бревна и запрещает меняющийся под слоем nylium
 *   1.18.0 — встроенный пресет стабильных блоков для мгновенной добычи в Two-layer
 *   1.19.0 — публичный Two-layer с безопасной палитрой, закреплёнными Cloud-артефактами
 *            и обновлённым пошаговым интерфейсом Companion
 *   1.19.1 — Two-layer экспортирует только конструкцию арта, без основания, дорожек и разметки
 *   1.19.2 — Companion автоматически и безопасно выдаёт карту только на контрольных точках
 *   1.19.3 — Two-layer усиливает безопасность карты/Cloud-пары и честно показывает настройки блока перепада
 *   1.20.0 — полностью автоматическая защищённая карта на контрольных точках и атомарное приватное сохранение Cloud
 *   1.20.1 — стабильная палитра Two-layer, ясный статус данных карты и безопасный Cloud-backfill
 *   1.21.0 — большие Two-layer арты: детерминированный bundle всех карт, Cloud и выбор части в Companion;
 *            компактная плашка MapKluss Companion с гайдом в Telegram
 */
export const VERSION = 'v1.21.0';
